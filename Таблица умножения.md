# 🧮 Таблица умножения.

Я учу таблицу умножения. Просто смотреть на неё в учебнике — скучно. А вот **запустить код**, поменять число и увидеть результат — совсем другое дело.

В этом разделе я решила сделать таблицы для чисел: 2, 7 и 8.

---

## Пример 1: таблица для числа 2

**Код:**
```java
public class Practice {
    public static void main(String[] args) {
        System.out.println(numbers());
    }

    public static String numbers() {
        String result = "";
        int a = 2;  // число умножения
        for (int i = 1; i <= 10; i++) {
            int c = a * i;
            result += a + " x " + i + " = " + c + "\n";
        }
        return result;
    }
}
```
#### Пример вывода в консоли
2 x 1 = 2  
2 x 2 = 4  
2 x 3 = 6  
2 x 4 = 8  
2 x 5 = 10  
2 x 6 = 12  
2 x 7 = 14  
2 x 8 = 16  
2 x 9 = 18  
2 x 10 = 20  

---

## Пример 2: таблица для числа 7
**Почему 7?**  
Семёрка — моё слабое место. 7×6, 7×8, 7×9 — путаюсь до сих пор.

**Код:**

```java
public class Practice {
    public static void main(String[] args) {
        System.out.println(numbers());
    }

    public static String numbers() {
        String result = "";
        int a = 7;
        for (int i = 1; i <= 10; i++) {
            int c = a * i;
            result += a + " x " + i + " = " + c + "\n";
        }
        return result;
    }
}
```
#### Пример вывода в консоли
7 x 1 = 7  
7 x 2 = 14  
7 x 3 = 21  
7 x 4 = 28  
7 x 5 = 35  
7 x 6 = 42  
7 x 7 = 49  
7 x 8 = 56  
7 x 9 = 63  
7 x 10 = 70  

---

## Пример 3: таблица для числа 8

**Код:**

```java
public class Practice {
    public static void main(String[] args) {
        System.out.println(numbers());
    }

    public static String numbers() {
        String result = "";
        int a = 8;
        for (int i = 1; i <= 10; i++) {
            int c = a * i;
            result += a + " x " + i + " = " + c + "\n";
        }
        return result;
    }
}
```
#### Пример вывода в консоли  
8 x 1 = 8  
8 x 2 = 16  
8 x 3 = 24  
8 x 4 = 32  
8 x 5 = 40  
8 x 6 = 48  
8 x 7 = 56  
8 x 8 = 64  
8 x 9 = 72  
8 x 10 = 80  

---

## Что можно менять в коде
| Если хочу... | То меняю... |
|------|-------------|
| Изменить число | `int a = 2`; на `int a = 4;` (или 3, 5, 6, 9) |
| Начать не с `1`, а с `5` | `int i = 1` на `int i = 5` |
| Сделать таблицу сразу для всех чисел | Добавляю второй цикл (пока не трогаю, но знаю, что так можно) |

---
## Зачем всё это в моём репозитории?
- Это моя собственная тетрадка по таблице умножения. Не чужая, а моя  
- Вместо того чтобы искать таблицу в интернете, я открываю код  
- Код можно быстро менять под то, что нужно прямо сейчас  

⭐ Маленький совет себе:  
Не пытаюсь выучить всё сразу. Код всегда под рукой.  
Если однажды я захочу выучить сразу всю таблицу от 1 до 9, я знаю, как это сделать. Но пока так — проще и понятнее.
